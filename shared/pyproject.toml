[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "biohack-shared"
version = "0.1.0"
description = "Shared models for BioHack Debunker"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.6"]

[tool.hatch.build.targets.wheel]
packages = ["src/shared"]
